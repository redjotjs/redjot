name: Checks

on:
  push:
    branches: ["trunk"]

jobs:
  check:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v6

      - run: npm ci
      - run: npm run check
